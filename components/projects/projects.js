function ProjectsController(recipeService) {
    const ctrl = this;
}

  angular
    .module('Portfolio')
    .component('Projects', {
      template: `
      
    `, // or use templateUrl
      controller: ProjectsController,
      // bindings: {
      //   me: '<',
      //   onDelete: '&',
      //   onUpdate: '&'
      // }
    });